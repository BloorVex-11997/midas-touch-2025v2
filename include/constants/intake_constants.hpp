#pragma once

/* Ports */

const int INTAKE_PORT = 4;
const int ELEVATOR_PORT = 5;

/* Configuration */

// voltage applied to intake when intake is enabled [-127, 127]
const int INTAKE_VOLTAGE = 64;

// voltage applied to elevator when intake is enabled [-127, 127]
const int ELEVATOR_VOLTAGE = 64;